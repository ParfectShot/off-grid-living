// Export guide hooks

// Placeholder export to avoid "not a module" linter errors
// Replace with actual hook exports when available
export const useGuidePlaceholder = () => {
  return { data: null, loading: false, error: null };
}; 